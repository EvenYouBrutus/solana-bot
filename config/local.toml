mode = "paper"

[rpc]
# Configure independent production providers before live use.
http_endpoints = ["https://api.mainnet-beta.solana.com"]
websocket_endpoints = []
max_data_age_secs = 15
request_timeout_secs = 8

[strategy]
base_mint = "So11111111111111111111111111111111111111112"
min_wallet_score = 60.0
min_wallet_samples = 25
min_consensus_wallets = 2
min_signal_score = 65.0
min_token_age_secs = 86400
stop_loss_pct = 5.0
take_profit_pct = 12.0
trailing_stop_pct = 4.0
max_holding_minutes = 240

[economics]
round_trip_cost_threshold_pct = 3.0
min_expected_net_return_pct = 2.0
max_quote_age_secs = 3
uncertainty_haircut_pct = 1.0

[risk]
starting_capital_usd = 100.0
max_live_capital_usd = 25.0
max_concurrent_positions = 1
max_position_percent_of_equity = 5.0
max_position_percent_of_liquidity = 0.10
max_risk_per_trade_percent = 0.5
max_daily_loss_percent = 2.0
max_total_drawdown_before_kill_switch_pct = 5.0
cooldown_after_loss_minutes = 30
max_slippage_bps = 100
min_liquidity_usd = 50000.0
max_trades_per_day = 3

[execution]
provider = "jupiter"
jupiter_api_url = "https://api.jup.ag"
slippage_bps = 75
priority_fee_lamports = 10000
# JSON byte-array keypair from an environment variable, never source control.
live_signer_env = "SOLANA_BOT_KEYPAIR_JSON"
allowed_program_ids = []

[storage]
sqlite_path = "state/solana-bot.sqlite3"

[runtime]
# JSONL CandidateInput records. This is the explicit boundary for a verified
# on-chain/indexer collector; absent input means no orders are generated.
signal_feed_path = ""
poll_interval_secs = 2
paper_fill_haircut_bps = 25

[observability]
log_format = "json"
