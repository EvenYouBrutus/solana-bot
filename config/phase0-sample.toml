mode = "paper"

# A conservative provisional scenario, not a live-trading authorization.
# rust_decimal's string encoding avoids binary floating-point money values.
position_size_usd = "10"
avg_priority_fee_usd = "0.002"
avg_swap_fee_bps = "30"
avg_slippage_bps = "100"
avg_price_impact_bps = "20"
failed_tx_rate = "0.10"
avg_failed_tx_cost_usd = "0.002"
assumed_win_loss_ratio = "2"
assumed_avg_loss_pct = "10"